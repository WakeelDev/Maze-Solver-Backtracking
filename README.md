# Maze-Solver-Backtracking
A C++ program that solves a 2D maze using recursive backtracking. It reads the maze from a text file, finds the path from start (S) to exit (E), and outputs the solution and recursive trace.
